[project]
name = "cmnc-contracts"
version = "0.1.0"
description = "Shared contracts for ClassroomMikrotikNetControl services"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.4",
]

[build-system]
requires = ["uv_build>=0.9.0,<0.12.0"]
build-backend = "uv_build"
